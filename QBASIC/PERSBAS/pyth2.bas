DIM header AS STRING
DIM a AS SINGLE
DIM b AS SINGLE
DIM c AS SINGLE

CLS
PRINT
PRINT "This is a Pythagorean Therum Calculator"
INPUT "Enter In A^2: ", a
INPUT "Enter B^2: ", b
PRINT SQR(a ^ 2 + b ^ 2)