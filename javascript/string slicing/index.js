// string slicing = creating a substring from a portion of another string
//                  string.slice(start, end)

let fullname = "Jake Schnieder"
let firstname = fullname.slice(0,4);
h3.textContent = firstname;